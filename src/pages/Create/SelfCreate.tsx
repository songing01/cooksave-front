import Header from "@components/Header/Header";

//식재료를 직접 등록하는 페이지
const SelfCreate = () => {
  return (
    <div>
      <Header isBack={true} title="식재료 등록" />
    </div>
  );
};

export default SelfCreate;
